<div class="container-fluid no-pad landing_two"></div>
<div id="profiles">
  <h2>Listing gyms</h2>

  <table class="table profiler">
    <thead>
      <tr>
        <th>Image</th>
        <th>Name</th>
        <th>Street</th>
        <th>City</th>
        <th>State</th>
        <th>Zip</th>
      </tr>
    </thead>

    <tbody>
      <% @gyms.each do |gym| %>
        <tr>
          <td><%= image_tag gym.image_url.to_s if gym.image_url.present? %></td>
          <td><%= gym.name %></td>
          <td><%= gym.street %></td>
          <td><%= gym.city %></td>
          <td><%= gym.state %></td>
          <td><%= gym.zip %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <div class="col-md-12 no-pad"><%= link_to 'New Gym', new_gym_path, :class => "new" %></div>
</div>
